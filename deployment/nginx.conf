http {
    server {
        listen 80;
        root  /usr/share/nginx/html;
        include /etc/nginx/mime.types;

        location /admin/ {
            root  /usr/share/nginx/html;
            try_files $uri $uri/ /admin/index.html;
        }
    }
}